services:
  db:
    image: pgvector/pgvector:pg17
    environment:
      POSTGRES_DB: god
      POSTGRES_USER: god
      POSTGRES_PASSWORD: god
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
